tools = 'gcc'
SConscript( 'SConscript', exports=['tools'], build_dir='build-'+tools, duplicate=0 )
#tools = 'crossmingw'
#SConscript( 'SConscript', exports=['tools'], build_dir='build-'+tools, duplicate=0 )
